# Same-origin browser pages need no CORS configuration.
# Set only when a separate trusted frontend must call this API.
ALLOWED_ORIGINS=https://example.invalid

# Set to 1 behind an HTTPS reverse proxy to emit HSTS.
FORCE_HTTPS=0
